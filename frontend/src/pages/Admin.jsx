import React,{useState} from 'react';import axios from 'axios';export default function Admin(){const[user,setUser]=useState('');const[pass,setPass]=useState('');const[token,setToken]=useState('');const API=import.meta.env.VITE_API_BASE||'http://localhost:4000';async function login(){const r=await axios.post(API+'/api/admin/login',{user,pass});setToken(r.data.token);alert('Logged in');}async function assign(){await axios.post(API+'/api/teams/assign',{teamCount:2},{headers:{Authorization:'Bearer '+token}});alert('Teams Assigned');}return(<div className='container'><h2>Admin</h2><div className='card'><input placeholder='User' value={user} onChange={e=>setUser(e.target.value)}/><input placeholder='Password' type='password' value={pass} onChange={e=>setPass(e.target.value)}/><button onClick={login} className='btn'>Login</button><button onClick={assign} className='btn ghost'>Auto Assign Teams</button></div></div>)}