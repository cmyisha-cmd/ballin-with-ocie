import React,{useEffect,useState} from 'react';import axios from 'axios';export default function Bracket(){const[teams,setTeams]=useState([]);const API=import.meta.env.VITE_API_BASE||'http://localhost:4000';useEffect(()=>{axios.get(API+'/api/teams').then(r=>setTeams(r.data));},[]);return(<div className='container'><h2>Team Bracket</h2><div className='card'>{teams.map(t=>(<div key={t.name}><strong>{t.name}</strong>: {t.players.map(p=>p.name).join(', ')}</div>))}</div></div>)}