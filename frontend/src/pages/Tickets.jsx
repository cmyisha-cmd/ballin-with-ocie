import React,{useState} from 'react';import axios from 'axios';export default function Tickets(){const[name,setName]=useState('');const[count,setCount]=useState('');const API=import.meta.env.VITE_API_BASE||'http://localhost:4000';async function submit(){await axios.post(API+'/api/tickets',{name,count});alert('Thank you! Your tickets will be available at the Box Office');}return(<div className='container'><h2>Request Tickets</h2><div className='card'><input placeholder='Name' value={name} onChange={e=>setName(e.target.value)}/><input placeholder='Number of Tickets' type='number' value={count} onChange={e=>setCount(e.target.value)}/><button onClick={submit} className='btn'>Submit</button></div></div>)}