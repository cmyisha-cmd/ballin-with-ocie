{
  "name": "ballin-with-ocie-root",
  "private": true,
  "scripts": {
    "build:all": "npm run build --prefix ./frontend",
    "dev": "npm run dev --prefix ./frontend",
    "start": "node server/index.js"
  }
}
