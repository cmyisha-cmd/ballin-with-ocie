{
  "version": 2,
  "builds": [
    { "src": "frontend/vite.config.js", "use": "@vercel/static-build", "config": { "distDir": "dist" } }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "https://YOUR-RENDER-BACKEND.onrender.com/api/$1" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
