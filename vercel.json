{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install && cd ../server && npm install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://ballin-with-ocie.onrender.com/api/$1" },
    { "source": "/(.*)", "destination": "/" }
  ]
}
